.confirm-action {
  font-family: 'Montserrat', sans-serif;
  font-size: 14px
  padding: 0 46px
  padding-bottom: 36px
  
  display: flex
  align-items: center
  flex-direction: column
  
  span {
    margin-top: 20px
  }
  
  .btn-confirm {
    margin-top: 40px
    text-align: center
  }
}