@import '~/variables';

.user-account{
    display: flex;
}

.user-account__wrapper{
  box-shadow: 0 2px 20px rgba(0, 5, 26, 0.1);
  height: inherit;
  width: 100%;
  overflow-x: hidden;
}

.user-account__content {
  margin-left: 48px;
  padding-top: 44px;
}

.user-account__profile{
    width: 362px;
    height: inherit;
    flex-shrink: 0;
}
  
.workroom__block-header {
  display: flex
  align-items: center
  margin: 28px 0 0 8px
  span {
    display: block
    font-weight: 600
    margin-right: 8px
  }
}
  
.workroom__block-underline {
  width: 100%
  height: 1px

  margin-top: 40px
  background-color: rgba(219, 219, 219, 1)
  &.calendar {
    
  }
}

@media screen and (max-width: $full-screen) {
    .user-account__profile{
        width: 282px;
    }
}

@media screen and (max-width: $middle-screen) {
    .user-account__profile{
        width: 255px;
    }
}
